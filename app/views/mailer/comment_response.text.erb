Someone has responded to your comment on XtextCasts. Here is the response.

---
From: <%= raw @comment.user.name %>

<%= raw @comment.content %>
---

To view the full comment: <%= episode_url(@comment.episode, :view => "comments") %>

To unsubscribe from future emails from XtextCasts, just click this link:
<%= unsubscribe_url(@user.generated_unsubscribe_token) %>
